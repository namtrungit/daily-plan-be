# Copy to .env and adjust (do not commit .env).

PORT=3000

# PostgreSQL — matches docker/.env.example when using Compose Postgres on localhost
DATABASE_URL=postgresql://dailyplan:changeme@localhost:5432/dailyplan
